[
  {
    "id": 201,
    "question": "Kube-OVN 的 kube-ovn-controller 透過哪個介面操作 OVN Northbound DB？",
    "options": [
      "直接執行 ovn-nbctl CLI 命令",
      "透過 libovsdb client（OVNNbClient）以 OVSDB 協定操作",
      "透過 Kubernetes API 間接更新 ConfigMap",
      "直接寫入 etcd 中的 OVN schema"
    ],
    "answer": 1,
    "explanation": "Controller 結構體中的 OVNNbClient 欄位類型為 ovs.NbClient，底層使用 libovsdb 以 OVSDB 協定直接連接 OVN Northbound DB，並在記憶體維護 NB DB 快取，避免每次操作都查詢資料庫。"
  },
  {
    "id": 202,
    "question": "在 Kube-OVN 的 VPC CRD 中，哪個欄位決定哪些 namespace 的 Pod 屬於這個 VPC？",
    "options": [
      "spec.defaultSubnet",
      "spec.namespaces",
      "spec.staticRoutes",
      "spec.enableExternal"
    ],
    "answer": 1,
    "explanation": "VpcSpec 中的 Namespaces []string 欄位明確列出可以使用此 VPC 的 namespace 清單。Controller 根據此欄位更新對應 namespace 的 annotation，讓 Pod 分配到正確的 VPC 網路。"
  },
  {
    "id": 203,
    "question": "Kube-OVN 的 IPAM 模組在 controller 重啟後如何重建 IP 分配狀態？",
    "options": [
      "從 OVN Southbound DB 讀取 Logical Switch Port 的 addresses 欄位重建",
      "從 Kubernetes 中的 IP CRD 物件讀取重建記憶體 map",
      "從 etcd 的特定 key prefix 讀取持久化的 IP 狀態",
      "重啟後 IPAM 狀態從零開始，等待 Pod 重新申請"
    ],
    "answer": 1,
    "explanation": "IPAM 是純記憶體結構，每次 IP 分配成功後 controller 會建立對應的 IP CRD（kind: IP）。Controller 重啟時從所有存在的 IP CRD 重建 PodToIP、IPToPod 等 map，確保不重複分配。"
  },
  {
    "id": 204,
    "question": "當 Subnet 的 gatewayType 設定為 centralized 且指定多個 gatewayNode 時，Kube-OVN 採用什麼機制分散出口流量？",
    "options": [
      "Round-robin DNS",
      "Kubernetes Service 的 sessionAffinity",
      "OVN Logical Router 的 ECMP（Equal-Cost Multi-Path）路由",
      "Linux IPVS 的 weighted round-robin"
    ],
    "answer": 2,
    "explanation": "指定多個 gatewayNode 時，controller 的 reconcileEcmpCentralizedSubnetRouteInDefaultVpc 函數會為每個 gateway 節點建立等價路由（ECMP），OVN Logical Router 自動在多條路徑間分散流量。"
  },
  {
    "id": 205,
    "question": "Kube-OVN 的 Security Group 底層使用 OVN 的哪兩個機制來實作？",
    "options": [
      "Logical Switch + Address Set",
      "Port Group + ACL",
      "Logical Router Policy + NAT",
      "Load Balancer + Health Check"
    ],
    "answer": 1,
    "explanation": "每個 SecurityGroup CRD 對應一個 OVN Port Group，加入同一 Security Group 的 Pod port 都會加入此 Port Group。ACL 規則附加在 Port Group 上，自動套用到所有成員 port，規則以 ovnnb.ACLDirectionToLport 和 ACLDirectionFromLport 分別控制 ingress 與 egress。"
  },
  {
    "id": 206,
    "question": "ovn.kubernetes.io/ingress_rate annotation 在 Kube-OVN 中限制的是哪個方向的流量？",
    "options": [
      "Pod 送出（egress）的流量",
      "Pod 收到（ingress）的流量",
      "Node 層級的所有入站流量",
      "Service 的 ClusterIP 入站流量"
    ],
    "answer": 1,
    "explanation": "從 Pod 視角，ingress_rate annotation 限制 Pod 收到的流量速率（單位 Mbps）。值得注意的是 daemon 實作中將 annotation 方向與 OVS interface 方向做了互換，但對使用者而言語義是「限制 Pod 收到的頻寬」。"
  },
  {
    "id": 207,
    "question": "BGP speaker 的 local 宣告策略（announcePolicyLocal）與 cluster 策略的主要差異是什麼？",
    "options": [
      "local 策略只宣告 IPv4 路由，cluster 策略同時宣告 IPv4 和 IPv6",
      "local 策略只在有 Pod 實際運行的節點上宣告，cluster 策略在所有 speaker 節點宣告",
      "local 策略使用 iBGP，cluster 策略使用 eBGP",
      "local 策略宣告 /32 主機路由，cluster 策略宣告 subnet CIDR"
    ],
    "answer": 1,
    "explanation": "subnet.go 中定義：announcePolicyLocal 讓 Pod IP 只從實際承載 Pod 的節點宣告，使流量直接到達 Pod 所在節點。announcePolicyCluster 則在所有 speaker 節點宣告，流量可能從任一節點進入再透過 OVN overlay 路由到目標 Pod。"
  },
  {
    "id": 208,
    "question": "Kube-OVN 的 IPAM GetStaticAddress 函數在遇到指定 IP 已被其他 Pod 佔用時，會回傳哪個錯誤？",
    "options": [
      "ErrNoAvailable",
      "ErrNoSubnet",
      "ErrConflict",
      "ErrOutOfRange"
    ],
    "answer": 2,
    "explanation": "pkg/ipam/ipam.go 中定義了四種 IPAM 錯誤：ErrOutOfRange（IP 超出 CIDR 範圍）、ErrConflict（IP 已被其他 Pod 佔用）、ErrNoAvailable（無可用 IP）、ErrNoSubnet（subnet 不存在）。靜態 IP 衝突時回傳 ErrConflict。"
  },
  {
    "id": 209,
    "question": "kube-ovn-controller 的 subnetKeyMutex 欄位的主要用途是什麼？",
    "options": [
      "對 etcd 的寫入操作加鎖，防止並發更新 CRD",
      "確保同一個 subnet 在任何時刻只有一個 reconcile goroutine 在執行",
      "對 OVN NB DB 的 transaction 加鎖",
      "防止多個 controller 副本同時當選為 leader"
    ],
    "answer": 1,
    "explanation": "Controller 結構體中的 subnetKeyMutex keymutex.KeyMutex 為每個 subnet key 提供獨立的鎖，確保同一個 subnet 不會被兩個 worker goroutine 同時 reconcile，避免競態條件導致 OVN NB DB 狀態不一致。"
  },
  {
    "id": 210,
    "question": "Kube-OVN 的 Underlay 模式與 Overlay 模式的根本差異是什麼？",
    "options": [
      "Underlay 使用 VXLAN 封裝，Overlay 使用 Geneve 封裝",
      "Underlay 模式 Pod 流量以原生 VLAN（802.1Q）直接接入實體網路，Overlay 模式在節點間建立隧道封裝",
      "Underlay 模式只支援 IPv4，Overlay 模式支援 DualStack",
      "Underlay 模式由 kube-proxy 管理，Overlay 模式由 OVN 管理"
    ],
    "answer": 1,
    "explanation": "Overlay 模式使用 Geneve/VXLAN 隧道在節點間傳輸封包，Pod IP 不在物理網路可見。Underlay 模式的 Pod veth 橋接到 OVS bridge 並打上 VLAN tag，流量以 802.1Q 原生 tagged frame 送上實體交換機，Pod MAC/IP 對物理網路直接可見，不需解封裝，延遲接近 bare-metal。"
  },
  {
    "id": 211,
    "question": "在 Kube-OVN 的 Underlay 模式中，ProviderNetwork CRD 的 defaultInterface 欄位定義了什麼？",
    "options": [
      "OVN Northbound DB 的連接端點",
      "各節點上用來承接 Underlay 流量的預設實體網路介面名稱",
      "Pod 的預設 gateway IP",
      "NAT Gateway Pod 使用的外部介面"
    ],
    "answer": 1,
    "explanation": "ProviderNetwork 的 defaultInterface 欄位指定絕大多數節點上的實體介面名稱（如 eth1），controller 會在每個節點的 OVS 中建立對應的 bridge（如 br-net1）並將此介面設為 uplink port。對於介面名稱不同的特殊節點，可透過 customInterfaces 欄位個別指定。"
  },
  {
    "id": 212,
    "question": "Kube-OVN 用 OVN Load Balancer 取代 kube-proxy 的主要優勢是什麼？",
    "options": [
      "OVN LB 支援 UDP 負載均衡，iptables 不支援",
      "新增或移除 Endpoint 時只更新單一 LB 物件的 backends 欄位，不需要全量重寫 iptables 規則鏈",
      "OVN LB 可自動進行 TLS 終止",
      "OVN LB 支援 HTTP/2 層級的路由"
    ],
    "answer": 1,
    "explanation": "iptables/IPVS 方案在 Service 規模大時，任何 Endpoint 變化都需要重寫整張規則鏈（數萬條規則），對 Node 的 CPU 造成明顯影響。OVN LB 將每個 Service 的 VIP→Backends 映射獨立存儲，Endpoint 變化只更新對應 LB 物件，不影響其他 Service，規模可線性擴展。"
  },
  {
    "id": 213,
    "question": "當 Kubernetes Service 沒有健康的 Endpoint 時，Kube-OVN 的 OVN Load Balancer 如何處理打到 ClusterIP 的封包？",
    "options": [
      "封包被靜默丟棄（drop）",
      "封包被路由到預設 gateway",
      "OVN 回傳 TCP RST 或 ICMP port unreachable（REJECT）",
      "封包被轉發到 kube-proxy 處理"
    ],
    "answer": 2,
    "explanation": "OVN LB 的 VIP 若 backends 清單為空，OVN 在 datapath 中對這些封包執行 REJECT（回傳 TCP RST 或 ICMP port unreachable），讓客戶端立即收到連線失敗訊號。這比預設路由丟包更乾淨，避免客戶端等待 timeout。"
  },
  {
    "id": 214,
    "question": "Kube-OVN 的 VPC NAT Gateway 中，Gateway Pod 有幾張網卡？各連接到哪裡？",
    "options": [
      "一張網卡，連接到 VPC subnet",
      "兩張網卡，一張連接 VPC subnet（內網），一張連接 external subnet（外網）",
      "三張網卡，分別連接 VPC subnet、management subnet 和 external subnet",
      "一張網卡，透過 VLAN 區分內外網流量"
    ],
    "answer": 1,
    "explanation": "VPC NAT Gateway Pod 透過 Multus 掛載兩張額外網卡：第一張（VPC 側）連接到 VPC 內的 gateway subnet，使用 VPC 內的 IP（lanIp）作為 VPC 預設路由的出口；第二張（外部側）連接到 underlay external subnet，直接接入實體網路。流量從 VPC Pod 進入 Gateway Pod 的 VPC 側介面，SNAT 後從外部側介面送出。"
  },
  {
    "id": 215,
    "question": "IptablesEIP CRD 在 Kube-OVN 中實現了什麼功能？",
    "options": [
      "為 Service 分配一個 External IP，等同於 LoadBalancer 類型 Service",
      "讓特定 VPC Pod 擁有固定的外部 IP，入站流量 DNAT 到 Pod，出站流量 SNAT 為該 EIP",
      "為 Pod 分配一個叢集內部的靜態 IP",
      "讓 Pod 直接使用 BGP 宣告自己的 IP"
    ],
    "answer": 1,
    "explanation": "IptablesEIP 從 external subnet 的 CIDR 分配（或指定）一個外部 IP，controller 在 NAT Gateway Pod 上設定 iptables 規則：DNAT 讓外部流量打到 EIP 後轉到對應的 VPC Pod IP；SNAT 讓 Pod 出口流量使用 EIP 作為來源 IP。這使 Pod 擁有穩定的外部身分，跨重啟不變，滿足 IP 白名單需求。"
  }
]
